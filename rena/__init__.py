"""Server-side scaffolding for the RENA edge VLA service.

`messages.py` and the `Transport`/`Server` abstractions are intentionally a
duplicate of their counterparts in `rena-control/rena_navigation/vla/`. Once
the contract is stable we will extract the shared bits to a small pip package
and import it from both repos.
"""
